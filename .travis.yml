language: python
python:
  - "3.6"
  - "3.7"
  - "3.8"
install:
  - "pip install pipenv"
  - "pipenv install --dev --skip-lock"
  - "pipenv install coveralls"
script:
  - "make coverage-test lint check-fmt"
after_success:
  - coveralls --verbose
